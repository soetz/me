# MINI SCRIPTS EXECUTED BEFORE SHELL

# (just put `source $HOME/.me` inside your .bashrc or .zshrc or whatever
# and then every time you're asked to put something inside your .*rc, put
# it here instead − I don't know if it's good practice, it's just how I
# do it)

source $HOME/.aliases

source $HOME/.zsh-conf

source "/etc/profile.d/rvm.sh"
source $(dirname $(gem which colorls))/tab_complete.sh